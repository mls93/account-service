dependencies {
    compile project(":accounts-connection"),
            lib.guicePersist,
            lib.guiceServlet,
            lib.jettyServer,
            lib.jettyServlet

    compile group: 'org.hibernate', name: 'hibernate-core', version: '5.4.1.Final'
}
